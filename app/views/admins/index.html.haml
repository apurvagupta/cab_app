= render '/permissions'

-if @is_admin
  .row-fluid
    .span12
      %a.btn{href: new_admin_path, id: "New"}+
      %table{cellspacing: 50, id: 'table', :class=>"table table-striped table-hover table-bordered "}
        %th Name
        %th Contact No
        %th
        %th
        - @admins.each do |admin|
          %tr
            %td= admin.name
            %td= admin.contact_no
            %td
              %a{href: edit_admin_path(admin), id: 'Edit'}
                %i.icon-pencil
            %td
              %a{href: admin_path(admin) ,id: 'Delete' ,:method => :delete, :confirm => 'Are you sure?', :hidden => admin.status || admin.name == @user_id}
                %i.icon-trash
        %end


