%table{:cellspacing => 50}
  %th Name
  %th Contact No
  %th 
  %th 
  %hr
  - @admins.each do |admin|
    %tr
      %td= admin.name
      %td= admin.contact_no
      %td= link_to 'edit', edit_admin_path(admin)
      %td= link_to 'delete', admin_path(admin), :method => :delete, :confirm => 'Are you sure?'
  %end

= link_to 'new', new_admin_path